namespace I {
    export interface Data {
        ext: string,
        code: string,
        name: string
    }

    export interface postData {
        ext: string,
        code: string
    }

    export interface Config {
        host: string
        port: string
    }
}
